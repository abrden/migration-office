#define CATCH_CONFIG_MAIN  // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.hpp"

// Empty file with the purpose of defining CATCH_CONFIG_MAIN so Catch provides a main()